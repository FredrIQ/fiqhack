language: c

os:
    - linux
    - osx

compiler:
    - gcc
    - clang

# Disable aimake for now, for performance purposes
script:
    - rm _aimake_get_option.*
    - mkdir build installdir
    - cd build
    - ../aimake -i ../installdir --without=gui

notifications:
    irc: "chat.freenode.net#FIQHack"
